#1. Escreva um algoritmo que leia valores inteiros e encontre o
#maior e o menor deles. Termine a leitura se o usuário digitar
#zero (0);

num=1
numeros = []
while num!=0:
    num = int(input(' Digite o numero: '))
    numeros.append(num)
print(f' O menor número é o {min(numeros)} e o maior numero é o {max(numeros)}')
