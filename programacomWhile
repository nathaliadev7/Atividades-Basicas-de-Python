#Faça o mesmo que antes, porém, ao invés de ler 10 números, o programa deverá ler e somar números
#até,que o valor digitado seja 0:

soma=0
num=1
while num!=0:
    num = int(input(' Digite o numero: '))
    soma+=num
print(soma)
